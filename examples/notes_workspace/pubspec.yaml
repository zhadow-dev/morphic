name: notes_workspace
description: A real multi-window Morphic app — Notes list, Editor and Inspector.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  morphic: ^0.2.0-dev.8

dev_dependencies:
  flutter_lints: ^5.0.0
