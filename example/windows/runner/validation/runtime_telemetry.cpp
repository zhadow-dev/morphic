// PHASE 7D — RuntimeTelemetry is header-only (atomics + constants). This .cpp
// exists so the file is grouped with the rest of the validation/ TU set and
// future non-trivial helpers (e.g., snapshot serializer) have a home.
#include "validation/runtime_telemetry.h"
