name: morphic_quickstart
description: "Morphic quickstart — two native windows from one Flutter app, kept in sync over AppBus."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  morphic:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
